import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    maxHeight: 110,
    marginTop: 50,
    width: "100%",
    flexDirection: "row",
  },
});
